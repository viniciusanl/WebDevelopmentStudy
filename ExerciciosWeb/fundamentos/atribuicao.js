const a = 10
let b = 20

b += a // E a mesma coisa que b = b + a
console.log(b)
    // b vale 30

b -= a // E a mesma coisa que b = b - a
console.log(b)
    // b vale 20

b *= 2 // E a mesma coisa que b = b * 2
console.log(b)
    // b vale 40

b /= 2 // E a mesma coisa que b = b / 2
console.log(b)
    // b vale 20

b %= 2 // E a mesma coisa que b = b % 2
console.log(b)